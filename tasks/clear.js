"use strict";
// var gulp = require('gulp');
// var run  = require('gulp-run');

// this can be easily extracted into one line call
// without requiring plguin
// - npm install gulp-run
// run('clear').exec();

// BUG: Clear Task
// THIS IS NOT WORKING CORRECTCLY WHEN INSIDE TASKS FOLDER AS IT IS AUTOLOADED AND EXECUTED
// gulp.task('clear-terminal', function () {
//   run('clear').exec();
// });
